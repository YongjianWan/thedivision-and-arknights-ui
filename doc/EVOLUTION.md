# UI 母版 演进史 (Evolution Log)

> 记录从 "丑UI" 到 "战术界面系统" 的进化过程

---

## 📌 当前版本

**v2.1.0** — 基础设施修复完成，视觉层次重构中

---

## 🔍 理想和现实的差别（历史评估）

> 2026-01-15 ~ 01-20 期间的工程现状诊断记录，为后续修复提供基线。

### 理想设定（摘要）
- 设计定律：系统感界面、三层信息、强调色只用于焦点/告警/关键读数
- 视觉语法：L0-L3 边框层级、8pt 网格、模块化卡片
- Token 规范：`tokens.json` 是唯一真源
- 动效规则：同时动效 ≤2，背景层安静，Modal 打开时冻结背景

### 主要差距（2026-01-15 评估）

| # | 问题 | 影响 | 后续修复 |
|---|------|------|---------|
| 1 | Token 真源未落地 | `globals.css` 手工维护，无生成链路 | ✅ Phase 2 引入 `generate-tokens.mjs` |
| 2 | 强调色滥用 | 装饰性背景大量使用 accent，违背规范 | ✅ Phase 2 清理 |
| 3 | L3 数量无约束 | 同屏出现多个 L3 面板 | ✅ Phase 2 规范 |
| 4 | 8pt 网格执行不足 | 大量使用 12px/20px 非标准间距 | ✅ Phase 2 统一 spacing |
| 5 | 动效无统一控制 | 背景动效过多，无暂停机制 | ✅ Phase 1 引入 `body[data-overlay-open]` |
| 6 | 组件覆盖不全 | ListRow/Toast/Input 等缺失 | ✅ Phase 1 补齐 |
| 7 | 焦点规范不一致 | ring/边框/左侧线条混用 | ✅ Phase 2 统一 `.focus-ring` |
| 8 | props 接口不一致 | 多处调用不存在的 props | ✅ Phase 1 补齐 API |

---

## 🗓 演进时间线

### Phase 0: 原型混沌期 (v1.x)
**时间**: 2026-01-15 ~ 01-20  
**状态**: ✅ 已归档

- Next.js 项目初始化
- Tailwind + Tokens 系统搭建
- 33个基础组件野蛮生长
- **问题**: 各自为政，没有统一视觉语言

**关键提交**: `d07d3f8` "已修复：TOKEN 动效同一控制..."

---

### Phase 1: 美学补丁包 v1.1
**时间**: 2026-01-21  
**代号**: "幽灵层" (Ghost Layer)  
**状态**: ✅ 已完成

| 补丁 | 内容 | 组件 |
|------|------|------|
| **P1** | Ghost Layer | HUDMeter, TacticalPanel title |
| **P3** | Anchor Points | TacticalPanel L2/L3 边框层级 |
| **P4** | NoiseOverlay | 全局噪点层 |
| **P9** | Quantized Easing | Progress 组件步进动画 |

**关键提交**: `76c2d45` "feat: implement aesthetic patch v1.1"

**遗留问题**: 
- ❌ NoiseOverlay 后被移除，但导出未清理
- ❌ P1 Ghost Layer 只在部分组件实现

---

### Phase 1.5: 补丁包 v2 (交互增强)
**时间**: 2026-01-21
**代号**: "交互深度"
**状态**: ✅ 已完成

| 组件 | 状态 | 说明 |
|------|------|------|
| `HoldButton` | ✅ | 长按确认按钮，1500ms 量化进度条 |
| `Schematic` | ✅ | 线框图装饰，SVG 节点+流动光点 |
| `usePerformanceMode` | ✅ | 性能自适应，三档模式 |
| `clinical-copy` | ✅ | 临床文风词库，100+ 条 |

#### 落地详情

**HoldButton 长按确认**
- 默认 1500ms，10 格量化进度 (steps 缓动)
- danger 色系，完成时闪烁 + glitch 效果
- 预设变体: `HoldToDelete`, `HoldToReset`

**Schematic 行为信息图**
- SVG 节点 + 直角折线连接
- 流动光点动画模拟数据传输
- 节点类型: default/source/target/active/error
- 预设变体: `ServerTopology`, `DataFlow`

**usePerformanceMode**
- 自动检测：CPU/内存/移动端/`prefers-reduced-motion`
- 三档输出：full/reduced/minimal
- 配置项：blur/noise/particleCount/animationScale

**Clinical Copy**
- 词库：STATUS(20+)/ACTIONS(25+)/LABELS(20+)/PLACEHOLDERS(7+)
- 辅助函数：`getLoadingText()`, `formatClinicalTimestamp()`

---

### Phase 1.6: 补丁包 v3 (故障美学)
**时间**: 2026-01-21
**代号**: "受控故障"
**状态**: ✅ 已完成

| 组件 | 状态 | 说明 |
|------|------|------|
| `useGlitch` | ✅ | 可控故障 Hook，三档强度 |
| `StatusIndicator` | ✅ | 双重编码状态指示器（颜色+图标） |
| `TacticalIcons` | ✅ | 16 个战术图标，Filled 风格 |
| 量化缓动扩展 | ✅ | CSS + Framer Motion |

#### 落地详情

**useGlitch 受控故障**
- 三档强度: light/medium/heavy
- 严格触发：仅真实错误、网络超时、状态突变
- CSS: `glitch-shake-*` + `glitch-text-*` 色差效果

**StatusIndicator 双重编码**
- 5 种状态: ok/warn/err/busy/offline
- 色盲友好：颜色 + Filled 图标
- 变体: sm/md/lg，dotOnly 模式

**TacticalIcons 战术图标**
- 16 个图标: TargetLock/Signal/Radar/Uplink/Secure/Alert/Battery/...
- 全部 Filled/Solid 风格，锐利边角

**量化缓动扩展**
- CSS: `.number-tick`, `.fade-quantized`, `.hud-value-update`, `.skeleton-pulse-quantized`
- Framer Motion: `quantizedEase4/6/10`, `quantizedTransition`

---

### Phase 2: 架构修复期 (v2.0)
**时间**: 2026-02-07  
**状态**: ⚠️ **部分完成，文档记录中**

#### 诊断背景
在2026-02-06的全面代码审查中，发现6个根本性架构问题导致视觉效果打折。这不是组件代码质量问题，而是系统性基础设施bug。

#### Fix 1: 字体系统统一 ✅
- **问题**: `layout.tsx` 用 Next.js font optimization 加载 Roboto Condensed，设置 `variable: '--font-display'`。但 `tokens.css` 也定义了 `--font-display: 'DIN Alternate', 'Roboto Condensed', sans-serif`。两者互相覆盖，取决于加载顺序。组件里用法更混乱——有的写 `font-display`（tailwind class），有的写 `font-['DIN_Alternate','Roboto_Condensed',sans-serif]`（内联），有的写 `font-[var(--font-display)]`。
- **修复**: 删除 `tokens.css` 字体变量，全部交给 Next.js font loader + tailwind config。组件统一用 `font-display`、`font-mono` 这种 tailwind class。
- **状态**: 完成

#### Fix 2: 边框渲染修复 ✅
- **问题**: `--border-thin: 0.5px` 在非 Retina 屏上要么渲染成 0（消失），要么渲染成 1px（跟 base 一样）。这直接导致"为什么有些边框看不见"。
- **修复**: 改为 `1px`，用 opacity 和颜色来区分层级，而不是 sub-pixel width。
- **状态**: 完成

#### Fix 3: 背景色层级拉开 ✅
- **问题**: `#0B0E12`（base）和 `#101622`（elevated）的 luminance 差距只有 2-3%。人眼几乎分不出来，尤其叠了 backdrop-blur 和 noise overlay 之后。结果就是：所有面板看起来糊成一团。
- **修复**: 
  - base → `#070A0E` (更黑)
  - elevated → `#151D2E` (更亮，带蓝调)
  - overlay alpha 从 0.75 降到 0.6
- **状态**: 完成

#### Fix 4: 全屏叠加层清理 ⚠️
- **问题**: scanline（扫描线）+ NoiseOverlay（噪点）同时覆盖整个屏幕。两者都是 `pointer-events: none` + z-index 超高 + opacity: 0.03。单独看都很微妙，叠一起就让整个画面变得"脏"。而且两个 fixed 全屏元素一直在那里吃 GPU。
- **修复**: 从 `layout.tsx` 移除 NoiseOverlay，只保留 scanline
- **遗留**: 
  - ❌ `src/components/index.ts` 仍导出 NoiseOverlay (死代码)
  - ❌ `globals.css` 仍有 `.noise-global` 样式未清理
  - ❌ NoiseOverlay 组件文件本身还在目录里
- **状态**: **未完成**

#### Fix 5: 组件库统一 ✅
- **问题**: `page.tsx` 自己写了一个本地的 TacticalPanel 组件，没有 import `src/components/TacticalPanel`。这意味着花时间打磨的组件库根本没被用上。首页是"草稿"状态的内联组件。
- **修复**: 删掉 `page.tsx` 里的本地 TacticalPanel，import 真正的那个。
- **状态**: 完成

#### Fix 6: Tailwind 补全 ✅
- **问题**: tokens 有 `accentAlt: '#18D1FF'`，但 tailwind config 的 colors 里没有 `accent-alt`。组件要用只能写 `text-[var(--accent-alt)]` 这种 arbitrary value，没法用 `text-accent-alt` 这种正经 class。
- **修复**: 添加 `accent-alt` colors 映射
- **状态**: 完成

#### Fix 7: TacticalPanel 背景简化 ✅
- **问题**: 所有 TacticalPanel 都用 `bg-overlay(rgba 0.6)` + `backdrop-blur`，叠起来跟一团灰泥一样。backdrop-blur 应该只给真正的 overlay（Modal、Drawer、StatusBar）用，不是给每个面板都糊一层。
- **修复**: TacticalPanel 改用实色 elevated 背景，去掉全局 backdrop-blur
- **状态**: 完成

**关键提交**: `c00d180` "feat: update UI components and add new tactical elements"

---

### Phase 3: 补丁包 v2 (进行中)
**时间**: 2026-02-07 ~  
**代号**: "深度与氛围"

#### 补丁包 2: 交互增强
| 组件 | 状态 | 说明 |
|------|------|------|
| `HoldButton` | ✅ | 长按确认按钮 |
| `Schematic` | ✅ | 线框图装饰 |
| `usePerformanceMode` | ✅ | 性能自适应 |
| `clinical-copy` | ✅ | 临床文风词库 |

#### 补丁包 3: 故障美学
| 组件 | 状态 | 说明 |
|------|------|------|
| `useGlitch` | ✅ | 可控故障 Hook |
| `StatusIndicator` | ✅ | 双重编码状态指示器 |
| `TacticalIcons` | ✅ | 战术图标系统 |

#### 待完成 (Z-Depth 重构)
| 任务 | 优先级 | 状态 |
|------|--------|------|
| TacticalPanel elevation 系统 | P0 | ⏳ 待设计 |
| TechDecor 角落装饰扩展 | P1 | ⏳ 待设计 |
| 三层文字渲染 (Main/Shadow/Ghost) | P1 | ⏳ 待设计 |
| 临床文风组件落地 | P2 | ⏳ 待设计 |
| 清理 NoiseOverlay 死代码 | P2 | ❌ 未开始 |

---

## 🎯 设计原则演进

### v1.x: "看起来像"
- 橙色主题 ✅
- 扫描线 ✅
- 边框装饰 ✅

### v2.x: "感觉对"
- **Z-Depth 层次** ⏳ — 从平面到立体
- **不完美软件** ⏳ — glitch、加载感、故障
- **临床文风** ⏳ — 工业化文案
- **点线面构成** ⏳ — 数学化、非有机

### v3.x: "沉浸其中" (愿景)
- Diagetic UI — UI 是世界的一部分
- 实时数据感 — 内存、CPU、网络延迟显示
- 响应式氛围 — 根据系统状态改变整体色调

---

## 📁 补丁包命名规范

```
P1 = Ghost Layer      (幽灵层/文字效果)
P2 = Interaction      (交互增强)
P3 = Anchor Points    (锚点/角落装饰)
P4 = Atmosphere       (氛围层/噪点/扫描线)
P5 = Icons            (图标系统)
P6 = Animation        (动效系统)
P7 = Typography       (排版系统)
P8 = Color            (色彩系统)
P9 = Easing           (缓动函数)
```

---

## 🔧 技术债务清单

### 高优先级
- [ ] **NoiseOverlay 死代码清理**
  - `src/components/NoiseOverlay.tsx` 文件保留但无用
  - `src/components/index.ts` 第 47 行导出需删除
  - `src/app/globals.css` `.noise-global` 样式需删除
  
### 中优先级
- [ ] NodeGraph.tsx 硬编码字体 `'var(--font-mono)'`
- [ ] 组件 showcase 页面 (`app/components/page.tsx`) 硬编码字体过多

### 低优先级
- [ ] 评估 NoiseOverlay 是否值得复活（性能开销 vs 视觉效果）
- [ ] 声音系统 (useSound) — 需要音频资源
- [ ] 世界锚点 (ContentAnchor) — 复杂度高，需深度集成

---

## 🚀 下一步行动

### 立即 (今天)
1. 关闭 NoiseOverlay 死代码清理循环
2. 创建 Z-Depth 设计文档

### 短期 (本周)
1. TacticalPanel elevation 系统实现
2. TechDecor 角落装饰扩展

### 中期 (本月)
1. 临床文风组件落地 (Button clinical variant)
2. 三层文字渲染系统

### 长期 (愿景)
1. 与 Gateway 后端联调
2. 动态主题切换 (温度/湿度/威胁等级)

---

## 📋 附录：修复决策记录

### 2026-02-06 架构诊断会议纪要

**参与者**: AI Assistant + 开发者  
**背景**: UI视觉效果不达预期，需要系统性诊断  
**方法**: 代码审查 + 基础设施分析（非视觉调整）

**发现的问题清单**:
1. CSS Variable inconsistency — 组件使用混合的 var(--xxx) inline 和 Tailwind class
2. Homepage 重新定义 TacticalPanel — page.tsx 有本地内联版本而非使用组件库
3. Font loading issues — layout.tsx CSS 变量与 tokens.css 冲突
4. Incomplete Tailwind-to-CSS-var mapping — accent-alt 在 tokens 但不在 tailwind config
5. Sub-pixel border rendering — 0.5px 在非标屏上不稳定
6. Performance hit from backdrop blur — 多面板 backdrop-blur 吃 GPU
7. Flat visual hierarchy — 背景色层级差距太小
8. Constant full-screen overlays — scanline + noise 双层叠加
9. Cramped spacing — 面板间隙过小
10. Too many competing effects — ghost text + scanlines + noise + glitch 同时开启

**决策**: 先修基础设施（30分钟），再调视觉。基础设施修好后，组件本身已经能正确呈现。

---

## 📝 参考文档

- `doc/参考/全境封锁设定稿.txt` — 官方设计圣经（OCR修复版）
- `doc/achieved/补丁包2.txt` — P2 设计规范（已合并）
- `doc/achieved/补丁包3.txt` — P3 设计规范（已合并）
- `doc/achieved/修改过程.txt` — 完整开发日志（原始记录）
- `src/lib/clinical-copy.ts` — 临床文风词库

---

*Last updated: 2026-02-07*  
*Next review: Phase 3 完成后*
